{
	"$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
	"contentVersion": "1.0.0.0",
	"parameters": {
		"apiSettings": {
            "type": "object",
            "metadata": {
                "Description": "API version of the resource type"
            }
        },
        "networkSettings": {
            "type": "secureObject",
            "metadata": {
                "Description": "Network settings object"
            }
        },
		"offerType": {
			"type": "string",
			"metadata": {
				"description": "The Azure Cosmos offer type, by default Standard"
			},
			"allowedValues": [
				"Basic",
				"Standard"
			],
			"defaultValue": "Standard"
		},
		"name": {
			"type": "string",
			"metadata": {
				"description": "The Azure Cosmos instance name."
			}
		}
	},

	"resources": [
		{
			"type": "Microsoft.DocumentDB/databaseAccounts",
			"name": "[parameters('name')]",
			"apiVersion": "[parameters('apiSettings').documentDbApiVersion]",
			"location": "[resourceGroup().location]",
			"kind": "MongoDB",
			"scale": null,
			"properties": {
				"name": "[parameters('name')]",
				"databaseAccountOfferType": "[parameters('offerType')]",
				"consistencyPolicy": {
                    "defaultConsistencyLevel": "Session",
                    "maxIntervalInSeconds": 5,
                    "maxStalenessPrefix": 100
                },
				"isVirtualNetworkFilterEnabled": true,
				"virtualNetworkRules": [
					{
						"id": "[resourceId('Microsoft.Network/virtualNetworks/subnets', parameters('networkSettings').virtualNetworkName, parameters('networkSettings').subnetName)]"
					}
				],
				"ipRangeFilter": "[parameters('networkSettings').subnetPrefix]"
			}
		}
	]
}